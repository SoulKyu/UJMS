apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: test
spec:
  dnsNames:
  - test.soulkyu.net
  issuerRef:
    name: scaleway
    kind: ClusterIssuer
  secretName: example-com-tls